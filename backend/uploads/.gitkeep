# This file ensures the uploads directory is tracked by Git while keeping it empty
# The uploads directory is used by the application to store uploaded files
# All uploaded files are ignored by .gitignore for security reasons